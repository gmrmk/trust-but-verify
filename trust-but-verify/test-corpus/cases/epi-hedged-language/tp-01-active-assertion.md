<!-- trust-but-verify corpus specimen. Label + rationale: test-corpus/manifest.yaml -->
## Deployment notes

The new retry path should work for all upstream timeouts, so we can drop the
manual fallback.
